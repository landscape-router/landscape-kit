mirror.tuna = "Tsinghua TUNA"
mirror.aliyun = "Aliyun"
mirror.ustc = "USTC"
mirror.official = "Official"
family.debian = "Debian"
family.ubuntu = "Ubuntu"
family.fedora = "Fedora"
family.centos7 = "CentOS 7"
family.centos_stream = "CentOS Stream"
family.rocky = "Rocky Linux"
family.alma = "AlmaLinux"
family.arch = "Arch Linux"
mirror.os_release_unreadable = "cannot detect the distribution: %{path} is unreadable or missing"
mirror.distro_unsupported = "distribution %{id} is not supported; supported: Debian, Ubuntu, Fedora, CentOS 7/Stream, Rocky, AlmaLinux, Arch"
mirror.no_source_files = "no package source files were found on this host"
mirror.no_official_source = "no known official source URLs were found; nothing was changed"
mirror.no_backup = "no previous source backup exists; run `lkit set-mirror <MIRROR>` first"

set_mirror.requires_args = "specify a mirror (tuna, aliyun, ustc, official) or use --list/--show/--restore"
set_mirror.root_required = "root privileges are required to change package sources"
set_mirror.select_mirror = "Select a software mirror"
set_mirror.security_prompt = "Also replace the Debian security repository?"
set_mirror.security_keep = "Keep the official security repository (default)"
set_mirror.security_replace = "Also replace the security repository"
set_mirror.confirm_apply = "Switch the %{family} package sources to %{mirror}? Type `yes` to continue: "
set_mirror.confirm_restore = "Restore the backed-up original package sources? Type `yes` to continue: "
set_mirror.applied = "switched the %{family} package sources to %{mirror} (%{files} file(s) changed)"
set_mirror.no_change = "the %{family} package sources are already on %{mirror}; nothing was changed"
set_mirror.restored = "restored the original package sources from backup"
set_mirror.backup_at = "original sources backed up at %{path}"
set_mirror.skipped = "%{count} repository/repositories skipped (no baseurl)"
set_mirror.cancelled = "cancelled; nothing was changed"
set_mirror.list_header = "Available mirrors for %{family}:"
set_mirror.show_header = "Current package sources:"
