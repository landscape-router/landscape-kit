manage.installation_already_exists = "an installation already exists; use update, switch, repair, or reconcile"
manage.single_instance_refused = "an installation already exists; run `lkit uninstall` first (or use update, switch, repair, or reconcile)"
manage.command_requires_existing_installation = "this command requires an existing installation"
manage.force_cannot_be_combined = "--force cannot be combined with --repair-static, --repair-binary, or any --accept-* flag"
manage.install_root_is = "install root is"
manage.force_does_not_modify = "--force does not delete, move, overwrite, or quarantine any file"
manage.install_root_may_contain = "the install root may contain databases, credentials, certificates, backups, and user files"
manage.manually_delete_install_root = "manually inspect and delete the entire install root, then re-run `lkit install` without --force"
manage.takeover_requires_interactive_terminal = "network takeover requires an interactive terminal: %{error}"
manage.activated_awaiting_network_confirmation = "activated %{version} and is awaiting network confirmation"
manage.committed_first_install = "committed first install of %{version}"
manage.systemd_unit_registered = "systemd unit landscape-router.service is registered, enabled, and running"
manage.takeover_awaiting_confirmation = "network takeover is awaiting confirmation"
manage.reconnect_and_run_confirm = "reconnect to %{address} and run `lkit network confirm`"
manage.takeover_awaiting_confirmation_dhcp = "network takeover is awaiting confirmation; reconnect using the WAN DHCP lease and run `lkit network confirm`"
manage.management_interface = "management interface https://127.0.0.1:6443"
manage.confirm_before_rollback = "confirm the network takeover within %{minutes} minutes or the installation will be rolled back automatically"
manage.enter_admin_password = "Enter admin password"
manage.enter_flare_psk = "Enter the flare recovery psk (L2 recovery channel used when Landscape network configuration breaks; at least 12 characters)"
manage.old_manual_deployment_warning = "warning: found an old manual deployment at /root/.landscape-router; install does not migrate it and rejects deployments that could overwrite it or conflict on the fixed ports; use `lkit migrate --from /root/.landscape-router` to migrate"
manage.must_run_as_root = "must run as root (uid 0)"
