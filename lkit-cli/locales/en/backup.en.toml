backup.requires_existing_installation = "creating a backup requires an existing installation"
backup.created = "backup %{backup_id} created for version %{version} at %{path}"
backup.none_found = "no .lkb backups found under %{dir}"
backup.list_invalid = "%{count} backup(s) are invalid or permission-unsafe"
backup.verified = "backup %{backup_id} (%{label}) verified"
backup.remark_prompt = "remark (optional, at most 256 characters; press Enter for none): "
backup.auto_remark_switch = "automatic backup before switch"
backup.auto_remark_repair = "automatic backup before repair"
backup.auto_remark_restore = "automatic protection backup before restore"
backup.auto_remark_uninstall = "automatic protection backup before uninstall"
backup.auto_remark_reinit = "automatic protection backup before reinit"
backup.deleted = "backup %{backup_id} deleted"
backup.delete_confirm = "delete backup %{backup_id}? (type yes to continue)"
backup.delete_refused = "deletion cancelled"
backup.delete_requires_yes = "--yes is required in non-interactive mode to confirm the deletion"
backup.delete_invalid_id = "--backup %{id} does not match the backup ID format YYYYMMDD-HHMMSS-<8 lowercase hex>"
