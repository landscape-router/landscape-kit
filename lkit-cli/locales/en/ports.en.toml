ports.dns_port = "DNS port"
ports.http_management_port = "HTTP management port"
ports.https_management_port = "HTTPS management port"
ports.unable_read_listener_information = "Unable to read listener information: %{error}"
ports.port_unknown = "%{port} unknown"
ports.unable_read_all_listener_tables = "Unable to read all required kernel listener tables; the port cannot be confirmed as free"
ports.run_as_root_for_proc_net = "Run as root and confirm that the relevant /proc/net files are readable"
ports.port_not_listening = "%{port} not listening"
ports.port_free = "Port is free"
ports.port_occupied = "%{port} occupied"
ports.another_service_listening = "Another service is listening on this port, so Landscape cannot start the service"
ports.stop_service_or_move_port = "Stop the service using this port or move it to another port"
ports.listener_used_by = "%{protocol} %{address}:%{port} is used by %{comm} (pid=%{pid})"
ports.listener_owner_unreadable = "%{protocol} %{address}:%{port} is listening, but owner information is unreadable"
