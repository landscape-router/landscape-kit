dns.risk_note = "When Landscape starts its DNS service it may point /etc/resolv.conf at 127.0.0.1. If name resolution fails after Landscape stops, check this file first. This command is read-only and does not back up or modify the file."
dns.unable_read_symlink_target = "unable to read symlink target: %{err}"
dns.resolv_conf_symlink = "%{RESOLV_CONF} is a symlink -> %{target}"
dns.no_nameserver_entries = "no nameserver entries"
dns.no_usable_nameserver = "The configuration has no usable nameserver"
dns.symlink_recovery_risk = "The configuration file is a symlink, which creates a recovery risk"
dns.configuration_valid = "DNS configuration is valid"
dns.unreadable = "unreadable"
dns.resolv_conf_exists_cannot_read = "%{RESOLV_CONF} exists but cannot be read: %{err}"
dns.missing = "missing"
dns.resolv_conf_does_not_exist = "%{RESOLV_CONF} does not exist"
