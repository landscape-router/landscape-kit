existing.static_pages_restored = "static pages restored from the published release assets"
existing.backend_restored_and_verified = "the active backend binary was restored and verified"
existing.repair_failed_rolled_back = "repairing the backend failed; rolled back to the previous binary"
existing.repair_failed_rollback_failed = "repairing the backend failed and automatic rollback also failed: %{reason}; manual recovery is required"
existing.switched_to_version = "switched to version %{version}"
existing.backup_preserved = "backup %{backup_id} preserved in backups/"
existing.no_backup_created = "no backup was created (--allow-no-backup)"
existing.rolled_back_using_backup = " using backup %{id}"
existing.switch_failed_rolled_back = "switching to the target version failed; rolled back to %{version}%{backup}"
existing.switch_failed_rollback_failed = "switching failed and automatic rollback to %{version} also failed; manual recovery is required"
existing.observed_initialization_completion = "observed initialization completion; initialization is now complete"
existing.version_installed_and_verified = "version %{version} is already installed and verified"
existing.keep_modified_unit = "the managed service unit changed; keep it as-is? Type `yes`: "
existing.accept_service_change_ignored_unit = "warning: no managed service unit change detected; --accept-service-change ignored"
existing.accept_service_change_ignored_no_unit = "warning: no managed service unit exists; --accept-service-change ignored"
