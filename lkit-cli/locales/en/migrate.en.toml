migrate.confirm_plan = "prepare to migrate: source config directory %{source}, backend version %{version} (the backup does not upgrade), target service manager %{manager}. The old instance will be stopped and a managed installation will be created at the install root. The migration backup is kept under backups/. Continue? (type yes to continue)"
migrate.confirm_stop_foreground = "no systemd unit was found for the old instance; stop it manually (for example, terminate the foreground process or the shell that started it), then confirm"
migrate.committed = "migrated to version %{version}; migration backup %{backup_id} is preserved under backups/"
migrate.legacy_deployment_left = "the old deployment at %{source} was left untouched (its service is now stopped); remove it manually once you no longer need it"
migrate.requires_fresh_install_root = "the install root already has a committed installation; migrate requires a fresh directory"
migrate.single_instance_refused = "the lkit territory already has a committed installation; run `lkit uninstall` first"
migrate.failed_rolled_back = "migration of %{version} failed; the old instance was restored automatically"
migrate.failed_rollback_failed = "migration of %{version} failed and the old instance could not be restored automatically: %{reason}"
migrate.rollback_failed = "automatic rollback failed: %{rollback_error}"
migrate.validating_source = "validating the source configuration directory %{source}"
migrate.identifying_instance = "identifying the running instance on the fixed ports"
migrate.checking_export_api = "checking whether the running Landscape supports the config export API"
migrate.export_api_supported = "config export API is supported; the deployed backend version is %{version}"
migrate.creating_backup = "creating the migration backup (.lkb) from the running instance"
migrate.backup_created = "migration backup %{backup_id} created"
migrate.handing_over = "the migration plan is prepared; handing the switch over to the daemon worker"
migrate.interrupted_before_switch = "migration interrupted; the old deployment was not modified and no switch was performed"
migrate.stopping_old_instance = "stopping the old instance and taking over its systemd unit"
migrate.rebuilding_installation = "rebuilding the managed installation from the migration backup"
migrate.starting_managed_service = "registering and starting the managed service"
migrate.verifying_health = "waiting for the managed service to pass the health check"
migrate.tx_failed_mark_warning = "warning: could not mark the migration transaction as failed: %{error}"
migrate.old_instance_still_running = "warning: the old instance is still running; the managed service may fail to bind the fixed ports"
migrate.prepared_left_behind = "the prepared migration transaction %{transaction_id} was left behind; retrying `lkit migrate` will recover it and keep the migration backup"
migrate.cancelled_during_switch = "migration cancelled; rolling back to restore the old instance"
migrate.cancelled_rolled_back = "migration cancelled; the old instance was automatically restored (version %{version})"
