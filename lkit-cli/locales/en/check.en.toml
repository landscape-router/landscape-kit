check.runtime_identity_and_platform = "Runtime identity and platform"
check.kernel_version_and_capabilities = "Kernel version and capabilities"
check.resource_limits = "Resource limits"
check.required_commands_and_runtime_dependencies = "Required commands and runtime dependencies"
check.port_conflicts = "Port conflicts"
check.system_services_and_security_policy = "System services and security policy"
check.dns_configuration_risks = "DNS configuration risks"
check.lkit_daemon = "lkit resident service"
check.lkit_daemon_running = "running"
check.lkit_daemon_not_running = "not running"
check.lkit_daemon_running_reason = "the lkit daemon is running and ready for delegated operations"
check.lkit_daemon_blocks_delegation = "the lkit daemon is not running; install and lifecycle commands cannot be delegated to it"
check.lkit_daemon_non_root_note = "non-root sessions execute commands inline and do not require the daemon"
check.lkit_daemon_deploy_hint = "run `lkit self install` to deploy and start the daemon"
