dependency.ip_command = "ip command"
dependency.ip_command_exists_executable = "The ip command exists and is executable"
dependency.not_found = "not found"
dependency.ip_command_not_found = "The ip command was not found (provided by the iproute2 package)"
dependency.tc_command_and_bpf = "tc command and BPF support"
dependency.tc_exists_supports_bpf = "tc exists and supports BPF filters"
dependency.tc_filter_help_failed = "tc filter help failed (exit code %{exit_code})"
dependency.tc_help_mentions_bpf = "tc help does not mention bpf; BPF support is unavailable"
dependency.upgrade_iproute2 = "Upgrade iproute2 or install a build with BPF support"
dependency.unable_run_tc_filter_help = "Unable to run tc filter help: %{err}"
dependency.pppd_command = "pppd command"
dependency.pppd_command_exists = "The pppd command exists and is executable (used for PPPoE)"
dependency.pppd_not_found = "The pppd command was not found (required for PPPoE)"
dependency.container_runtime = "Container runtime (optional dependency)"
dependency.docker_or_podman_available = "docker or podman is available"
dependency.no_container_runtime = "Neither docker nor podman is available"
dependency.container_runtime_not_required = "A container runtime is not required for basic deployment; install and configure Docker or Podman before routing traffic to containers"
dependency.install_providing_ip_and_tc = "Install the package that provides `ip` and `tc` (usually `iproute2`, or `iproute` on Fedora/RHEL)"
dependency.install_ppp_package = "Install the `ppp` package that provides `pppd`; the package is usually not named `pppd`"
dependency.package_named_ppp = "; the package is named `ppp`, not `pppd`"
dependency.run_install_command = "Run `%{command}` as root (prefix it with `sudo` as a regular user)%{package_note}"
