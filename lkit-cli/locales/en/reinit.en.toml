reinit.requires_existing_installation = "reinit requires an existing installation with a valid install-state.json"
reinit.requires_systemd = "reinit requires the systemd service manager; this installation uses another manager"
reinit.requires_network_takeover = "reinit requires host network services already handed over to Landscape (stop/disable/mask detected); use `lkit install --takeover-network` for a first takeover"
reinit.blocked_by_pending_takeover = "network takeover %{transaction_id} is %{phase}; use `lkit network status`, `lkit network confirm`, or `lkit network rollback` first"
reinit.enter_admin_password = "Enter the new admin password for the reinitialized Landscape"
reinit.confirm_plan = "reinit version %{version}: wipe all configuration except the new credentials and the WAN %{wan} / LAN %{lan} plan; the database will be rebuilt. Continue? (yes to continue)"
reinit.lan_none = "none"
reinit.confirm_protection_backup = "A protection .lkb backup of the current configuration will be created first and is restorable with `lkit restore`. Continue?"
reinit.confirm_network_window = "After the service restarts, this session may disconnect; the reinit commits only after `lkit network confirm` from any reachable session, otherwise it rolls back within the confirmation window. Continue?"
reinit.warning_no_protection_backup = "warning: cannot create the protection backup; continuing without a portable configuration snapshot: %{error}"
reinit.rollback_failed = "automatic rollback failed: %{rollback_error}"
reinit.activated_awaiting_confirmation = "reinit %{version} activated; awaiting network confirmation"
reinit.protection_backup_id = "protection backup: %{backup_id}"
reinit.no_protection_backup = "no protection backup was created (--allow-no-backup)"
reinit.awaiting_confirmation = "reconnect to the new management address %{address} and run `lkit network confirm` to commit"
reinit.awaiting_confirmation_dhcp = "the new WAN uses DHCP; run `lkit network confirm` from a reachable session to commit"
reinit.confirm_before_rollback = "confirm before %{minutes} minutes or an unconfirmed reboot rolls the reinit back"
reinit.failed_rolled_back = "reinit %{version} failed; the previous installation was restored automatically"
reinit.failed_rollback_failed = "reinit %{version} failed and the previous installation could not be restored: %{reason}"
