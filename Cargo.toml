[package]
name = "lndp-client"
version.workspace = true
edition.workspace = true

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
landscape-proto = { path = "../proto" }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time"] }
